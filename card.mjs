const escapeXml = value => String(value).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&apos;' })[c]);
const archetypes = { Forex: 'LE STRATÈGE DES ÉQUILIBRES', Indices: 'LE LECTEUR DES CYCLES', 'Or & matières premières': 'L’OBSERVATEUR DES FORCES RÉELLES', Crypto: 'L’EXPLORATEUR DES NOUVEAUX FLUX' };
export function renderCard(m) {
 const p = { archetype: archetypes[m.market] };
  const nameSize = m.name.length > 30 ? 32 : m.name.length > 18 ? 43 : 60;
  const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="760" viewBox="0 0 1200 760"><defs><linearGradient id="bg" x2="1" y2="1"><stop stop-color="#202e35"/><stop offset="1" stop-color="#0b1418"/></linearGradient></defs><rect width="1200" height="760" rx="36" fill="url(#bg)"/><rect x="2" y="2" width="1196" height="756" rx="34" fill="none" stroke="#526053"/><g font-family="Arial,sans-serif"><text x="72" y="100" fill="#eeeeea" font-size="29" letter-spacing="3">M / MANUEL</text><text x="1000" y="100" fill="#d4ed91" font-size="23" letter-spacing="3">ORIGIN</text><text x="950" y="440" fill="#d4ed91" opacity=".07" font-size="300" font-family="Georgia,serif">${escapeXml(m.name[0].toUpperCase())}</text><text x="72" y="320" fill="#acbda4" font-size="19" letter-spacing="2">${escapeXml(p.archetype)}</text><text x="72" y="405" fill="#f0f1e9" font-size="${nameSize}">${escapeXml(m.name)}</text><text x="72" y="473" fill="#d4ed91" font-size="23">${escapeXml(m.market)} / ${escapeXml(m.style)}</text><path d="M72 555H1128" stroke="#526053"/><text x="72" y="610" fill="#a6b49f" font-size="20">MEMBRE N° ${String(m.id).padStart(5, '0')}</text><text x="1128" y="610" fill="#d4ed91" font-size="20" text-anchor="end">MACRO INTELLIGENCE</text><text x="72" y="693" fill="#8b9d8c" font-size="18">Inscription à la liste d’attente · L’accès au terminal sera proposé à son ouverture.</text></g></svg>`;
return svg;
}
